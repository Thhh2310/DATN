@model Pet_Care.Models.Appointment

@{
    ViewData["Title"] = "Appointment Details";
    Layout = "~/Areas/AdminQL/Views/Shared/_Admins.cshtml";
}

<div class="container mt-4">
    <div class="card shadow-lg">
        <div class="card-header bg-info text-white text-center">
            <h2>Appointment Details</h2>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3 fw-bold">@Html.DisplayNameFor(model => model.AppointmentDate)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.AppointmentDate)</dd>

                <dt class="col-sm-3 fw-bold">@Html.DisplayNameFor(model => model.AppointmentTime)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.AppointmentTime)</dd>

                <dt class="col-sm-3 fw-bold">@Html.DisplayNameFor(model => model.Status)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Status)</dd>
            </dl>
        </div>
        <div class="card-footer text-center">
            <a asp-action="Edit" asp-route-id="@Model?.AppointmentId" class="btn btn-warning px-4">Edit</a>
            <a asp-action="Index" class="btn btn-secondary px-4">Back to List</a>
        </div>
    </div>
</div>
